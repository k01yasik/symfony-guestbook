{% for conference in conferences %}
        <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('conference', {slug: conference.slug}) }}">{{ conference }}</a>
        </li>
{% endfor %}